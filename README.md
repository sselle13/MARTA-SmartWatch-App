# MARTA-SmartWatch-App
A lightweight Node.js backend that provides real‑time MARTA rail and CobbLinc bus arrival predictions for a WearOS smartwatch app. Automatically detects the transit system, decodes GTFS‑RT feeds, normalizes data, and returns a clean JSON countdown with urgency levels.
